// create an infinite scroll to item display
// when item is clicked openup a modal to pay -> if payment is succesfull -> download the pdf from the web3storage

const Buy = () => {
  return <div>Papers to Buy</div>;
};

export default Buy;
